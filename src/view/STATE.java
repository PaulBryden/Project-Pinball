package view;

public enum STATE {
    BUILD,
    RUN,
    ADD,
    REMOVE,
    MOVE,
    ROTATE,
    SELECT,
    GIZMO_CONNECT,
    KEY_CONNECT,
    RM_GIZMO_CONNECT,
    RM_KEY_CONNECT
}
